# Example frontend environment variables for Vite
# Copy to `.env.local` or set these variables in your deployment environment

# Base URL for backend API (include protocol and host, no trailing slash)
# Example: https://api.yourdomain.com or http://localhost:4000
VITE_API_BASE="http://localhost:4000"

# Optional: Thunderforest API key for transport basemap tiles (leave empty to use fallback)
VITE_THUNDERFOREST_KEY=""

# Any other VITE_* variables can be added here
