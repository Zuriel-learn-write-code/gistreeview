# Example backend environment variables
# Copy to `.env` and fill actual values for your environment

# Database connection (Postgres)
# Format: postgresql://user:password@host:port/dbname
DATABASE_URL=# Primary app DB connection (example for direct local Postgres)
DATABASE_URL=postgresql://postgres:IaWbJZ79nnp8FBex@db.ptimvcfiwjekwbnwuwgw.supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:akira123@localhost:5432/gisdbtree


# Application port
PORT=4000

# Comma-separated list of allowed CORS origins (case-insensitive). Examples:
# - Local development and deployed frontend
# ALLOWED_ORIGINS=http://localhost:5173,https://gistreeview.vercel.app
# If empty the server will fall back to a built-in default set in code.
ALLOWED_ORIGINS=

# Cloudinary (optional) - fill if you use Cloudinary for uploads
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Any other secrets (JWT, mail, etc.) can be added here
# JWT_SECRET=your_jwt_secret

# Optional flags used by some scripts
# DRY_RUN=1 will usually prevent destructive actions in scripts
DRY_RUN=0
